import * as React from 'react';
import { AppLayout } from 'layouts';
import { CharacterEditContainer } from 'pods/character-edit/character-edit.container';

export const CharacterEditScene = () => (
  <AppLayout>
    <CharacterEditContainer />
  </AppLayout>
);
