import * as React from 'react';
import { AppLayout } from 'layouts';
import { CharacterCollectionEditContainer } from 'pods/character-collection-edit';

export const CharacterCollectionEditScene = () => (
  <AppLayout>
    <CharacterCollectionEditContainer />
  </AppLayout>
);
